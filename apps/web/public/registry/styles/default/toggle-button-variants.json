{
  "name": "toggle-button-variants",
  "type": "registry:demo",
  "registryDependencies": [
    "toggle-button"
  ],
  "files": [
    {
      "path": "core/toggle-button/demos/variants.tsx",
      "content": "import { PinIcon } from \"@/lib/icons\";\nimport { ToggleButton } from \"@/registry/ui/default/core/toggle-button\";\n\nconst variants = [\"quiet\", \"outline\", \"accent\"] as const;\n\nexport default function Demo() {\n  return (\n    <div className=\"flex items-center gap-4\">\n      {variants.map((variant) => (\n        <ToggleButton\n          key={variant}\n          variant={variant}\n          defaultSelected={variant === \"accent\"}\n          aria-label=\"Toggle pin\"\n        >\n          <PinIcon className=\"rotate-45\" />\n        </ToggleButton>\n      ))}\n    </div>\n  );\n}\n",
      "type": "registry:demo",
      "target": ""
    }
  ]
}
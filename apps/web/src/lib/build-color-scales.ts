type Color = {
  baseColors: string[];
  ratios: number[];
  smooth: boolean;
  lightness: number;
  saturation: number;
};

type Colors = {
  neutral: Color;
  primary: Color;
  success: Color;
  warning: Color;
  danger: Color;
};
export const buildColorScales = (colors: Colors) => {
  
};

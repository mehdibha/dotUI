# Implementation Plans

Generated by the improve skill on 2026-06-11 (deep audit of `packages/colors` and its consumption boundary, planned at commit `05b44151`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Product context that applies to every plan: dotUI's north star is that every visual decision two design systems could disagree on is a user-configurable axis (see `CLAUDE.md`). Plans 006/007/008/009 implement axes the maintainer approved during the audit session; their builder-UI exposure is deliberately deferred to separate www-side work.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Recover the color-engine design spec into docs/research | P1 | S | — | DONE (2026-06-13) — `docs/research/2026-06-11-color-engine-spec.md` |
| 002 | Pin per-algorithm color output with characterization snapshots | P1 | S | — | DONE (2026-06-13) |
| 003 | Let registered custom algorithms through createTheme | P1 | M | — | DONE (2026-06-13) |
| 004 | Make an unknown `preserveSeedAt` step a loud error | P2 | S | — | DONE (2026-06-13) |
| 005 | Make the kernel own dark-mode generation | P1 | L | 001, 002 | TODO |
| 006 | Add `neutralChroma` + `backgroundChroma` knobs (tinted neutrals axis) | P2 | S-M | soft: 003, 005 | TODO |
| 007 | Add a `lightness` anchors knob to oklch/tailwind | P2 | S | soft: 005, 006 | TODO |
| 008 | Per-palette knob overrides (palette config objects) | P2 | M | 003 | TODO |
| 009 | Centralize on-color behind a theme-level `onColor` option | P3 | M | 002; land last | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned).

> 2026-06-13: plans 001–004 landed together (kernel-hardening foundation), alongside a new **wide-gamut `targetGamut` axis** that is *not* part of this audit — see `docs/research/2026-06-13-colors-modernization.md`, which also catalogs the remaining modern-SOTA gaps (alpha ramps, CVD tooling, harmony/image seeding, contrast-level slider). Plans 005–009 remain TODO.

## Dependency notes

- **005 requires 001 and 002.** 001 recovers the §4.7a design rationale that 005 supersedes (and appends its `> Decision:` line to); 002's mode-split snapshots are the gate proving 005 changed only `contrast`/`material` dark output. Do not run 005 without them.
- **008 requires 003** — 003 establishes the schema shape (`customThemeOptionsSchema` reusing `generativePalettes`) that 008 then extends in one place.
- **"soft" dependencies are conflict-avoidance, not logic**: 006/007 edit the same `oklch.ts` lines as 005, so landing them after 005 avoids rebase churn; either order is correct.
- **009 lands last** — it touches the producer return contract and the same `theme.ts` loop as 005/008; sequencing it last keeps every byte-identical snapshot gate meaningful.
- 004 is independent and can land at any point.
- Shared gate for 003/005/006/007/008/009: default-config output must stay byte-identical (snapshots + `pnpm build:registry` producing zero diff). Plan 005 is the only one allowed to change snapshots, and only `contrast`/`material` **dark** entries plus the kernel's oklch/tailwind dark entries — its plan file specifies the exact matrix.

## Findings considered and rejected (do not re-audit)

- **colorjs.io → culori hot-path swap**: the seam is documented in `shared/color.ts` and generation is single-digit ms per theme; not worth doing now.
- **Status-seed drift between kernel and www** (subagent report): false — `www/src/registry/theme/color-config.ts:83-86` already imports `SEMANTIC_COLORS` from the kernel.
- **"No contrast formula UI"** (subagent report): false — the wcag2/apca select exists at `www/src/modules/create/color-knobs.tsx:185-190`; only the per-step `ratios` array editor is missing (deferred with the other builder-UI gaps below).
- **Showcase-bundle semantic-token duplication**: real, but it lives in publisher/bundle tooling with a planned rewrite; `CLAUDE.md` says don't deepen it.
- **Runtime monotonicity guard in the www dark-reversal**: superseded — plan 005 deletes the reversal entirely.
- **Per-mode `paletteOverride` silently ignored by the `fixed` producer**: trivial impact, by-design-adjacent; not worth a plan.
- **www dependency vulnerabilities** (35 via fumadocs→next, none in `packages/colors`): out of this audit's scope; flagged to the maintainer as a separate task.

## Deferred (out of this audit's scope — needs its own www-layer pass)

Builder/registry-theme gaps that cap the kernel's flexibility but belong to a www audit: fixed six-palette set (`palettes.ts` / `SEED_FIELDS`), fixed semantic vocabulary hardcoded to default step names (`semantics.ts` — makes custom `steps` a trap), no `steps` UI, no Material `tones` editor (`color-knobs.tsx:204`), no contrast `ratios` editor, no per-mode (dark-lightness) editor, no simple/advanced toggle. Plans 005–009 are the kernel prerequisites for most of these.

import { tv } from "tailwind-variants";

export const focusRing = tv({
  base: "outline outline-border-focus forced-colors:outline-[Highlight] outline-offset-2 outline-0 focus-visible:outline-2",
});

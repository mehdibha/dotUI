import React from "react";
import { SearchIcon } from "lucide-react";
import type { IconNode } from "@/types/icons";
import { icons as lucideIcons } from "@/autogenerated/icons/lucide-icons";
import { icons as simpleIcons } from "@/autogenerated/icons/simple-icons";
import { IconsList } from "./icons-list";
import { Input } from "./ui/input";

type Library = "lucide-icons" | "simple-icons";
interface IconsExplorerProps {
  libraries: Library[];
}

export const IconsExplorer = (props: IconsExplorerProps) => {
  const { libraries } = props;

  const icons = [
    ...(libraries.includes("lucide-icons") ? lucideIcons : []),
    ...(libraries.includes("simple-icons") ? simpleIcons : []),
  ];

  return (
    <div>
      <div className="relative">
        <Input className="w-full pl-12" placeholder={`Search icons`} />
        <SearchIcon
          size={18}
          className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 transform text-muted-foreground"
        />
      </div>
      <IconsList icons={icons as { name: string; icon: IconNode }[]} className="mt-4" />
    </div>
  );
};

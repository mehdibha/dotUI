import type { ColorArea as AriaColorArea } from "react-aria-components";

/**
 * A color area allows users to adjust two channels of an RGB, HSL or HSB color value
 * against a two-dimensional gradient background.
 */
export type ColorAreaProps = React.ComponentProps<typeof AriaColorArea>;

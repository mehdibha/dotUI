---
title: Installation
description: Learn how to add dotUI to your project.
---

<InstallationCandidates>

<InstallationCandidate name="A">

dotUI requires [React 19](https://react.dev), [Tailwind CSS 4](https://tailwindcss.com), and [TypeScript 5](https://www.typescriptlang.org).

<Steps>

### Build your system in the Studio

Compose your design system in the [Studio](/studio) — colors, typography, icons, density, radius, component styles — and preview it on live components. The export dialog gives you an `init` command with your theme baked in.

### Run init

Run the command from the export dialog, or initialize with the default theme:

```npm
npx shadcn@latest init https://dotui.org/r/init
```

This sets up the shadcn CLI, installs your design tokens, and registers the `@dotui` registry in `components.json`.

### Add components

```npm
npx shadcn@latest add @dotui/button
```

Every component installs pre-styled to your system.

</Steps>

</InstallationCandidate>

<InstallationCandidate name="B">

Requires [React 19](https://react.dev), [Tailwind CSS 4](https://tailwindcss.com), and [TypeScript 5](https://www.typescriptlang.org).

<Steps>

### Build your system

Compose your design system in the [Studio](/studio) and open **Export** — it gives you an `init` command with your theme baked in.

Or initialize with the default theme:

```npm
npx shadcn@latest init https://dotui.org/r/init
```

### Run init

Run the command in your project root. It sets up the shadcn CLI, installs your theme, and registers the `@dotui` registry in `components.json`.

### Add components

```npm
npx shadcn@latest add @dotui/button
```

Every component installs pre-styled to your system.

</Steps>

</InstallationCandidate>

<InstallationCandidate name="C">

Requires [React 19](https://react.dev), [Tailwind CSS 4](https://tailwindcss.com), and [TypeScript 5](https://www.typescriptlang.org).

<Steps>

### Build your system

Compose your design system in the [Studio](/studio) — colors, typography, icons, density, radius, per-component styles — and preview every change on live components. When it looks right, open the export dialog: it gives you an `init` command with your theme baked in.

### Initialize your project

Run the command from the export dialog in your project root — or start with the default theme:

```npm
npx shadcn@latest init https://dotui.org/r/init
```

This sets up the shadcn CLI, installs your theme, and registers the `@dotui` registry in `components.json`.

### Add components

Every component installs pre-styled to your system:

```npm
npx shadcn@latest add @dotui/button
```

</Steps>

</InstallationCandidate>

<InstallationCandidate name="D">

Requires [React 19](https://react.dev), [Tailwind CSS 4](https://tailwindcss.com), and [TypeScript 5](https://www.typescriptlang.org).

## Build in the Studio

Compose your design system in the [Studio](/studio) — colors, typography, icons, density, radius, per-component styles — and preview every change on live components. The export dialog generates your setup command with your theme baked in.

<StudioCta />

## Add components

Run the `init` command from the export dialog in your project root, then add any component — each one installs pre-styled to your system:

```npm
npx shadcn@latest add @dotui/button
```

</InstallationCandidate>

</InstallationCandidates>

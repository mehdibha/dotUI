---
title: Color Swatch
description: A color swatch displays a color sample.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/ColorSwatch.html
wip: true
---

<InteractiveDemo
  name="color-swatch"
  controls={[
    {
      type: 'string',
      name: 'color',
      defaultValue: '#ff0000',
    },
  ]}
/>

## Installation

```npm
npx shadcn@latest add @dotui/color-swatch
```

## Usage

Use color swatches to display a color sample.

```tsx
import { ColorSwatch } from '@/ui/color-swatch'
```

```tsx
<ColorSwatch color="#ff0000" />
```

## Value

The `color` prop accepts a hex or CSS color string, or a `Color` object from `parseColor`. Translucent colors render over a checkerboard so their transparency shows through.

```tsx
import { parseColor } from 'react-aria-components'
;<ColorSwatch color={parseColor('#7f0000aa')} />
```

## Examples

<Examples className="sm:grid-cols-2 lg:grid-cols-3">
  <Example name="color-swatch/demos/default" title="Default" />
  <Example name="color-swatch/demos/color-palette" title="Color Palette" />
  <Example name="color-swatch/demos/in-button" title="In Button" />
</Examples>

## API Reference

### ColorSwatch

<Reference name="color-swatch" />

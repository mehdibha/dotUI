---
title: Modal
description: A modal displays content in a layer that overlays the page content.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/Modal.html
---

import {
  ModalPlayground,
  modalControls,
} from "@dotui/registry/ui/modal/playground";

<PageTabs>
  <PageTabPanel id="overview">

    <Demo name="modal/demos/basic" />

    ## Installation

    ```npm
    npx shadcn@latest add @dotui/modal
    ```

    ## Usage

    Use modals to display content in a layer that overlays the page content.

    ```tsx
    import { Button } from "@/components/ui/button";
    import { Dialog, DialogContent } from "@/components/ui/dialog";
    import { Modal } from "@/components/ui/modal";
    ```

    ```tsx
    <Dialog>
      <Button>Open Modal</Button>
      <Modal>
        <DialogContent>Modal content</DialogContent>
      </Modal>
    </Dialog>
    ```

    ## Playground

    Use the controls below to experiment with different modal props and see the live preview and code update.

    <InteractiveDemo component={ModalPlayground} controls={modalControls} />

    ## API Reference

    | Prop              | Type        | Default | Description                                             |
    | ----------------- | ----------- | ------- | ------------------------------------------------------- |
    | `isDismissable`   | `boolean`   | `false` | Whether the modal can be dismissed by clicking outside. |
    | `isKeyboardDismissDisabled` | `boolean` | `false` | Whether the modal can be dismissed with the Escape key. |
    | `children`        | `ReactNode` | -       | The content of the modal.                               |

    | Event             | Type                        | Description                                      |
    | ----------------- | --------------------------- | ------------------------------------------------ |
    | `onOpenChange`    | `(isOpen: boolean) => void` | Handler that is called when the open state changes. |

    ## Accessibility

    ### Keyboard interactions

    | Key      | Description                      |
    | -------- | -------------------------------- |
    | `Tab`    | Moves focus within the modal.    |
    | `Escape` | Closes the modal.                |

  </PageTabPanel>

  <PageTabPanel id="examples">
    <Example name="modal/demos/basic" />
  </PageTabPanel>
</PageTabs>

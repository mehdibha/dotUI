---
title: Number Field
description: A number field allows a user to enter a number value with a keyboard or increment/decrement with step buttons.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/NumberField.html
---

import {
  NumberFieldPlayground,
  numberFieldControls,
} from "@dotui/registry/ui/number-field/playground";

<PageTabs>
  <PageTabPanel id="overview">

    <Demo name="number-field/demos/default" />

    ## Installation

    ```npm
    npx shadcn@latest add @dotui/number-field
    ```

    ## Usage

    Use `NumberField` to allow users to enter a numeric value with keyboard or increment/decrement buttons.

    ```tsx
    import { NumberField } from "@/components/ui/number-field";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/field";
    ```

    ```tsx
    <NumberField>
      <Label>Quantity</Label>
      <Input />
    </NumberField>
    ```

    ## Playground

    Use the controls below to experiment with different number field props and see the live preview and code update.

    <InteractiveDemo component={NumberFieldPlayground} controls={numberFieldControls} />

    ## API Reference

    | Prop            | Type                                                                                                  | Default | Description                                                                                               |
    | --------------- | ----------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------- |
    | `formatOptions` | `Intl.NumberFormatOptions`                                                                            | -       | Formatting options for the value displayed in the number field.                                           |
    | `isWheelDisabled` | `boolean`                                                                                           | -       | Enables or disables changing the value with scroll.                                                       |
    | `value`         | `number`                                                                                              | -       | The current value (controlled).                                                                           |
    | `defaultValue`  | `number`                                                                                              | -       | The default value (uncontrolled).                                                                         |
    | `minValue`      | `number`                                                                                              | -       | The smallest value allowed for the input.                                                                 |
    | `maxValue`      | `number`                                                                                              | -       | The largest value allowed for the input.                                                                  |
    | `step`          | `number`                                                                                              | -       | The amount that the input value changes with each increment or decrement "tick".                          |
    | `isDisabled`    | `boolean`                                                                                             | -       | Whether the input is disabled.                                                                            |
    | `isReadOnly`    | `boolean`                                                                                             | -       | Whether the input can be selected but not changed by the user.                                            |
    | `isRequired`    | `boolean`                                                                                             | -       | Whether user input is required on the input before form submission.                                       |
    | `isInvalid`     | `boolean`                                                                                             | -       | Whether the input value is invalid.                                                                       |
    | `name`          | `string`                                                                                              | -       | The name of the input element, used when submitting an HTML form.                                         |
    | `autoFocus`     | `boolean`                                                                                             | -       | Whether the element should receive focus on render.                                                       |

    | Event           | Type                              | Description                                                     |
    | --------------- | --------------------------------- | --------------------------------------------------------------- |
    | `onChange`      | `(value: number) => void`         | Handler that is called when the value changes.                  |
    | `onFocus`       | `(e: FocusEvent<Target>) => void` | Handler that is called when the element receives focus.         |
    | `onBlur`        | `(e: FocusEvent<Target>) => void` | Handler that is called when the element loses focus.            |
    | `onFocusChange` | `(isFocused: boolean) => void`    | Handler that is called when the element's focus status changes. |
    | `onKeyDown`     | `(e: KeyboardEvent) => void`      | Handler that is called when a key is pressed.                   |
    | `onKeyUp`       | `(e: KeyboardEvent) => void`      | Handler that is called when a key is released.                  |

    | Data attribute         | Description                                                        |
    | ---------------------- | ------------------------------------------------------------------ |
    | `[data-focused]`       | Whether the number field is focused, either via a mouse or keyboard. |
    | `[data-focus-visible]` | Whether the number field is keyboard focused.                      |
    | `[data-disabled]`      | Whether the number field is disabled.                              |
    | `[data-invalid]`       | Whether the number field is invalid.                               |
    | `[data-readonly]`      | Whether the number field is read only.                             |

    ## Accessibility

    ### Keyboard interactions

    | Key              | Description                              |
    | ---------------- | ---------------------------------------- |
    | `ArrowUp`        | Increments the value by the step amount. |
    | `ArrowDown`      | Decrements the value by the step amount. |
    | `Page Up`        | Increments the value by a larger amount. |
    | `Page Down`      | Decrements the value by a larger amount. |
    | `Shift+ArrowUp`  | Increments the value by a larger amount. |
    | `Shift+ArrowDown`| Decrements the value by a larger amount. |
    | `Home`           | Sets the value to the minimum.           |
    | `End`            | Sets the value to the maximum.           |

  </PageTabPanel>

  <PageTabPanel id="examples">
    <Example name="number-field/demos/default" />
    <Example name="number-field/demos/sizes" />
    <Example name="number-field/demos/label" />
    <Example name="number-field/demos/description" />
    <Example name="number-field/demos/error-message" />
    <Example name="number-field/demos/format-options" />
    <Example name="number-field/demos/disabled" />
    <Example name="number-field/demos/read-only" />
    <Example name="number-field/demos/required" />
    <Example name="number-field/demos/uncontrolled" />
    <Example name="number-field/demos/controlled" />
  </PageTabPanel>
</PageTabs>

---
title: Button
description: Displays a button or a component that looks like a button.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/Button.html
---

<InteractiveDemo
  name="button"
  controls={[
    { name: "children", type: "string", defaultValue: "Button" },
    "variant",
    {
      name: "size",
      type: "enum",
      options: ["xs", "sm", "md", "lg", "xl"],
      defaultValue: "md",
    },
    "isDisabled",
    "isPending",
  ]}
/>

## Installation

```npm
npx shadcn@latest add @dotui/button
```

## Usage

A button triggers an action. Its label names the action; add an icon for extra context.

```tsx
import { Button } from "@/components/ui/button"
```

```tsx
<Button>Button</Button>
```

## Variants

Use the `variant` prop to set the visual style of the button.

<Demo name="button/demos/variants" />

## Sizes

Use the `size` prop to set the size of the button.

<Demo name="button/demos/sizes" />

## With icon

Add the `data-icon="inline-start"` or `data-icon="inline-end"` attribute to the icon for the correct spacing.

<Demo name="button/demos/prefix-and-suffix" />

## Icon buttons

Use the `isIconOnly` prop for icon-only buttons.<br/>
Provide an `aria-label` to give the button an accessible name.

<Demo name="button/demos/icon-only" />

## Pending

Use the `isPending` prop to show a loading indicator while an action completes.

<Demo name="button/demos/loading" />

## Link buttons

The Button component always represents a button semantically. To create a link that visually looks like a button, use the `LinkButton` component instead.

<Demo name="button/demos/link-button" />

## API Reference

### Button

<Reference name="button" />

### LinkButton

<Reference name="link-button" />

---
title: Date Field
description: A date field allows users to enter and edit date values using a keyboard.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/DateField.html
---

import {
  DateFieldPlayground,
  dateFieldControls,
} from "@dotui/registry/ui/date-field/playground";

<PageTabs>
  <PageTabPanel id="overview">

    <Demo name="date-field/demos/default" />

    ## Installation

    ```npm
    npx shadcn@latest add @dotui/date-field
    ```

    ## Usage

    Use `DateField` to allow users to enter and edit date values using a keyboard.

    ```tsx
    import { DateField } from "@/components/ui/date-field";
    import { DateInput } from "@/components/ui/input";
    import { Label } from "@/components/ui/field";
    ```

    ```tsx
    <DateField>
      <Label>Date</Label>
      <DateInput />
    </DateField>
    ```

    ## Playground

    Use the controls below to experiment with different date field props and see the live preview and code update.

    <InteractiveDemo component={DateFieldPlayground} controls={dateFieldControls} />

    ## API Reference

    | Prop              | Type                                                                                                  | Default     | Description                                                                                               |
    | ----------------- | ----------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
    | `value`           | `DateValue \| null`                                                                                   | -           | The current value (controlled).                                                                           |
    | `defaultValue`    | `DateValue \| null`                                                                                   | -           | The default value (uncontrolled).                                                                         |
    | `minValue`        | `DateValue`                                                                                           | -           | The minimum allowed date that a user may select.                                                          |
    | `maxValue`        | `DateValue`                                                                                           | -           | The maximum allowed date that a user may select.                                                          |
    | `granularity`     | `'day' \| 'hour' \| 'minute' \| 'second'`                                                             | `'day'`     | Determines the smallest unit that is displayed in the date picker.                                        |
    | `hourCycle`       | `12 \| 24`                                                                                            | -           | Whether to display the time in 12 or 24 hour format.                                                      |
    | `hideTimeZone`    | `boolean`                                                                                             | -           | Whether to hide the time zone abbreviation.                                                               |
    | `placeholderValue`| `DateValue`                                                                                           | -           | A placeholder date that influences the format of the placeholder shown when no value is selected.         |
    | `isDisabled`      | `boolean`                                                                                             | -           | Whether the input is disabled.                                                                            |
    | `isReadOnly`      | `boolean`                                                                                             | -           | Whether the input can be selected but not changed by the user.                                            |
    | `isRequired`      | `boolean`                                                                                             | -           | Whether user input is required on the input before form submission.                                       |
    | `isInvalid`       | `boolean`                                                                                             | -           | Whether the input value is invalid.                                                                       |
    | `name`            | `string`                                                                                              | -           | The name of the input element, used when submitting an HTML form.                                         |
    | `autoFocus`       | `boolean`                                                                                             | -           | Whether the element should receive focus on render.                                                       |

    | Event           | Type                              | Description                                                     |
    | --------------- | --------------------------------- | --------------------------------------------------------------- |
    | `onChange`      | `(value: DateValue) => void`      | Handler that is called when the value changes.                  |
    | `onFocus`       | `(e: FocusEvent<Target>) => void` | Handler that is called when the element receives focus.         |
    | `onBlur`        | `(e: FocusEvent<Target>) => void` | Handler that is called when the element loses focus.            |
    | `onFocusChange` | `(isFocused: boolean) => void`    | Handler that is called when the element's focus status changes. |
    | `onKeyDown`     | `(e: KeyboardEvent) => void`      | Handler that is called when a key is pressed.                   |
    | `onKeyUp`       | `(e: KeyboardEvent) => void`      | Handler that is called when a key is released.                  |

    | Data attribute         | Description                                                      |
    | ---------------------- | ---------------------------------------------------------------- |
    | `[data-focused]`       | Whether the date field is focused, either via a mouse or keyboard. |
    | `[data-focus-visible]` | Whether the date field is keyboard focused.                      |
    | `[data-disabled]`      | Whether the date field is disabled.                              |
    | `[data-invalid]`       | Whether the date field is invalid.                               |
    | `[data-readonly]`      | Whether the date field is read only.                             |

    ## Accessibility

    ### Keyboard interactions

    | Key              | Description                                                    |
    | ---------------- | -------------------------------------------------------------- |
    | `Tab`            | Moves focus between date segments.                             |
    | `ArrowUp`        | Increments the focused segment value.                          |
    | `ArrowDown`      | Decrements the focused segment value.                          |
    | `ArrowLeft`      | Moves focus to the previous segment.                           |
    | `ArrowRight`     | Moves focus to the next segment.                               |

  </PageTabPanel>

  <PageTabPanel id="examples">
    <Example name="date-field/demos/default" />
    <Example name="date-field/demos/sizes" />
    <Example name="date-field/demos/label" />
    <Example name="date-field/demos/description" />
    <Example name="date-field/demos/error-message" />
    <Example name="date-field/demos/placeholder" />
    <Example name="date-field/demos/granularity" />
    <Example name="date-field/demos/hour-cycle" />
    <Example name="date-field/demos/time-zones" />
    <Example name="date-field/demos/hide-time-zone" />
    <Example name="date-field/demos/prefix-and-suffix" />
    <Example name="date-field/demos/disabled" />
    <Example name="date-field/demos/read-only" />
    <Example name="date-field/demos/required" />
    <Example name="date-field/demos/uncontrolled" />
    <Example name="date-field/demos/controlled" />
  </PageTabPanel>
</PageTabs>

---
title: Toggle Button
description: A toggle button allows a user to toggle a selection on or off.
links:
  - label: Docs
    href: https://react-spectrum.adobe.com/react-aria/ToggleButton.html
---

<InteractiveDemo
  name="toggle-button"
  controls={[
    { name: "children", type: "string", defaultValue: "Pin" },
    "variant",
    "size",
    "isIconOnly",
    "isDisabled",
  ]}
/>

## Installation

```npm
npx shadcn@latest add @dotui/toggle-button
```

## Usage

A toggle button switches a selection on or off, like pinning or starring an item.

```tsx
import { ToggleButton } from "@/components/ui/toggle-button"
```

```tsx
<ToggleButton>Pin</ToggleButton>
```

## Variants

Use the `variant` prop to set the visual style of the toggle button.

<Demo name="toggle-button/demos/variants" />

## Sizes

Use the `size` prop to set the size of the toggle button.

<Demo name="toggle-button/demos/sizes" />

## With icon

Add the `data-icon="inline-start"` or `data-icon="inline-end"` attribute to the icon for the correct spacing.

<Demo name="toggle-button/demos/prefix-and-suffix" />

## Icon buttons

Use the `isIconOnly` prop for icon-only toggle buttons.<br/>
Provide an `aria-label` to give the button an accessible name.

<Demo name="toggle-button/demos/icon-only" />

## Selection

Use `defaultSelected` to set the initial state, or `isSelected` with `onChange` to control it.

<Demo name="toggle-button/demos/controlled" />

## API Reference

<Reference name="toggle-button" />

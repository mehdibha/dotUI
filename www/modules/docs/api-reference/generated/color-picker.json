{
  "name": "ColorPickerProps",
  "props": {
    "defaultValue": {
      "type": "string | Color",
      "detailedType": "string | Color | undefined",
      "description": "The default value (uncontrolled)."
    },
    "value": {
      "type": "string | Color",
      "detailedType": "string | Color | undefined",
      "description": "The current value (controlled)."
    },
    "defaultOpen": {
      "type": "boolean",
      "detailedType": "boolean | undefined",
      "description": "Whether the overlay is open by default (uncontrolled)."
    },
    "onOpenChange": {
      "type": "((isOpen: boolean) => void)",
      "detailedType": "((isOpen: boolean) => void) | undefined",
      "description": "Handler that is called when the overlay's open state changes."
    },
    "isOpen": {
      "type": "boolean",
      "detailedType": "boolean | undefined",
      "description": "Whether the overlay is open by default (controlled)."
    },
    "onChange": {
      "type": "((value: Color) => void)",
      "detailedType": "((value: Color) => void) | undefined",
      "description": "Handler that is called when the value changes."
    },
    "slot": {
      "type": "string | null",
      "detailedType": "string | null | undefined",
      "description": "A slot name for the component. Slots allow the component to receive props from a parent component.\nAn explicit `null` value indicates that the local props completely override all props received from a parent."
    },
    "children": {
      "type": "ChildrenOrFunction<ColorPickerRenderProps>",
      "description": "The children of the component. A function may be provided to alter the children based on component state."
    }
  }
}

"use client"

/* Alert — the inline notice (axes/alert.ts); there are no controls yet. */

import type { Studio } from "../state"

export function AlertSection(_props: { studio: Studio }) {
  return null
}

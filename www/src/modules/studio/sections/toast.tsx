"use client"

/* Toast — the floating notice (axes/toast.ts). There are no controls yet. */

import type { Studio } from "../state"

export function ToastSection(_props: { studio: Studio }) {
  return null
}

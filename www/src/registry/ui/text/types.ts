import type * as TextPrimitives from 'react-aria-components/Text'

/**
 * Text represents a slotted text element within a parent component,
 * such as a label or description inside a field.
 */
export interface TextProps extends TextPrimitives.TextProps {}

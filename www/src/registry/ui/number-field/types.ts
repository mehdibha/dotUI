import type { NumberField as AriaNumberField } from "react-aria-components";

/**
 * A number field allows a user to enter a number, and increment or decrement the value using stepper buttons.
 */
export interface NumberFieldProps extends React.ComponentProps<typeof AriaNumberField> {}

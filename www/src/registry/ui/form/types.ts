import type * as FormPrimitives from 'react-aria-components/Form'

/**
 * A form groups inputs together and handles submission and native, accessible
 * validation. Built on React Aria's `Form`, so it works with browser-native
 * validation out of the box and accepts server-side `validationErrors`.
 */
export interface FormProps extends FormPrimitives.FormProps {}

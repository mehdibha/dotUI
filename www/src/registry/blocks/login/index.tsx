// www-side barrel for the login block variants. Never shipped — the publisher
// resolves the selected variant from `meta.params.variant.files` into a single
// canonical `components/login.tsx`.
export { default as LoginCentered } from './base.centered'
export { default as LoginSplit } from './base.split'
export { default as LoginMinimal } from './base.minimal'

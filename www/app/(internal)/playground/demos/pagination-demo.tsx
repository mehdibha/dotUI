"use client";

import React from "react";

// Pagination component not available in registry
export function PaginationDemo() {
  return (
    <div className="flex items-center justify-center p-8 text-fg-muted">
      <p className="text-sm">Pagination component not available</p>
    </div>
  );
}
